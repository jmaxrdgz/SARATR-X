# config.yaml

# TODO: Make config loggable with lightning

model:
  name: SARATRX
  in_chans: 3
  mgf_kens: [9, 13, 17]
  resume: null
  norm_pix_loss: false              # Norm pix loss does not work with SAR

data:
  train_data: "capella/chips_512"
  img_size: 224
  dataset_std_dev: 1.57
  num_workers: 4

train:                              # SARATR-X Training hyperparameters
  seed: 42
  n_gpu: "auto"
  epochs: 200                       # 200
  warmup_epochs: 5                  # 5
  batch_size: 64                    # 800
  lr: 1.5e-4                        # 1.5e-4
  weight_decay: 0.05                # 0.05
  optimizer_momentum:
    beta1: 0.9                      # 0.9
    beta2: 0.95                     # 0.95
  clip_grad: 5.0