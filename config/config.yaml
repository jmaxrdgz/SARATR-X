# config.yaml

model:
  name: SARATRX
  in_chans: 3
  mgf_kens: [9, 13, 17]
  resume: null

data:
  train_data: "path/to/train/data"
  img_size: 224
  dataset_std_dev: 1.57

train:
  seed: 42
  batch_size: 32